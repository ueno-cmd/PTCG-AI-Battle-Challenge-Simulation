# ルカリオexデッキ combat.py切り出し・energy_score無効化考慮修正 最終ブランチ全体レビュー

- 日付: 2026-07-20
- 対象ブランチ: `feature/lucario-combat-split`
- レビュー範囲: `e7525e0..dc89074`（全6タスク完了時点、8コミット）
- レビュアー: opusモデルによる最終ブランチ全体レビュー（各タスクは実装計画に沿ってサブエージェント駆動で実施し、タスクごとの個別レビューは全件承認済み）

## 検証内容

全1681行の差分パッケージを精査し、`main.py`/`combat.py`/`constants.py`の最終状態を確認、`op_active_nullifies_ex`のフラグ伝搬を`agent()`から末端まで追跡、`uv run pytest -q`を実行（560 passed）、ビルドスクリプトを最終状態のツリーに対して実行し構文的に正しい単一ファイルが生成されることを確認した。

## 結論（先に要点）

**Ready to merge = With fixes**。Critical/Important指摘なし。Minor4件のうち2件を修正済み（コミット`f118ced`）、残り2件は対応不要と判定。

## 強み

- モジュール分割の依存方向が正しい：`constants.py`（依存なし）→`combat.py`（constantsに依存）→`main.py`（両方に依存）。循環importなし
- `card_table`明示引数化の制約を完全に遵守：`combat.py`内の全参照（`_calc_attack_damage`・`prize_count`・`pokemon_score`含む）が暗黙のモジュール間グローバル参照ゼロで、引数として一貫して伝搬されている
- Task4の3つの修正が正しく合成される：`agent()`→`_score_option`→`_score_card_option`(ATTACH_FROM)・`_score_attach_option`の全経路でフラグが正しく伝搬され、`-150`減点（修正1）と`+500`抑制（修正3）が異なる項に作用し二重計算なく積み上がることを確認
- 修正が最小限：既に正しく機能していたOgerpon_ex分岐の`+150`には手を加えず、対称な`-150`をRiolu/Mega_Lucario_ex分岐に追加しただけ
- 既存テストのimport箇所（`lm.X`形式、416+箇所）が計画通りゼロ変更で通過
- 実バトルログのID（86898758等）に紐づいた回帰テストと、カードデータ突き合わせテストにより裏付けが強い

## 指摘事項

### Critical（必須修正）
なし

### Important（要修正）
なし。全6タスクの機能が揃っており、テストは通過し、ビルド出力は有効。

### Minor（あれば対応）

1. **`main.py`の未使用`EnergyType` import** — `_calc_attack_damage`が`combat.py`へ移動した後に取り残されていた。**修正済み**（`f118ced`でimportリストから削除）
2. **`EPSILON`/`_rng`が`combat.py`・`main.py`の両方に存在（重複）** — それぞれ`calc_attack_plan`のメガブレイブ探索・`BossOrdersPolicy`で独立して使われており、共有オブジェクトである必要はない（テスト時は`rng`引数でスタブ注入する設計のため）。正当な設計判断と判定し**対応不要**
3. **ビルドスクリプトの正規表現が将来の単一行import記法に対して脆弱** — 現状の3箇所の内部importは全て括弧付き複数行形式のため実害なし。既存テスト`test_build_output_has_no_internal_package_imports`が異常を検知できる点も踏まえ、`cg.api`のimportが誤って剥ぎ取られないことを確認する陽性テストを追加（**修正済み**、`f118ced`で`test_build_output_preserves_cg_api_imports`を追加）
4. **Task4-1の`attacker1=True`+`op_active_nullifies_ex=True`複合ケースの専用テスト欠如** — `-50`・`-150`は独立した無条件減算のため、個別テストの組み合わせで算術的に安全性が担保されている。**対応不要**

### 追加所見（欠陥ではない）

修正3（Rock_Fighting_Energyの+500抑制）は、アクティブのOgerpon ex（exカテゴリ）が無効化持ち相手と対面した場合にも抑制がかかる。検証の結果、この場合でもOgerpon exは`+150`/`+10`/`+80`項で十分に高スコアを維持し、ベンチのMega Lucario ex（`-150`適用後）に競り負けることはないため実害はない。壁デッキ対面でロック闘エネルギーを非exのソルロックへ誘導すること自体はむしろ望ましい副次効果と判断した。

## 推奨事項

- （対応済み）未使用importの削除・ビルドスクリプトの陽性テスト追加
- 任意：`main.py`にも`combat.py`と対になる短いモジュールdocstringを追加し、分割後の責務（トレーナーズカードのスコアリング＋エージェント入口）を明記すると、将来のセッションが分割意図を素早く把握できる

## 判定

**Ready to merge: Yes**（Minor指摘2件を修正済み、残り2件は対応不要と判定した後の最終状態として）

**理由:** モジュール分割は`card_table`をグローバル漏れなく一貫して伝搬しており、3つのバグ修正は正しく合成され実ログ紐づけの回帰テストで裏付けられている。テストスイート561件全PASS、ビルドスクリプトは最終ツリーに対して有効な単一ファイルを生成する。残る指摘はいずれも実害のない軽微な項目のみ。
