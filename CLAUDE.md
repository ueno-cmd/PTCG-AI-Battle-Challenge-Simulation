このファイルは本プロジェクトにおけるAIの行動指針です。最上位命令として常に遵守すること。

---

## プロジェクト概要

**ステアリングファイル**：`docs/steering/` 以下に配置（AIが自動生成）

---

## 1. コミュニケーション

- **言語**：全て日本語で応答すること（成果物・コメント含む）
- **トーン**：親しみやすく、論理的で分かりやすい説明を心がける
- **専門用語**：難しい言葉はカッコ書きで補足するか噛み砕いて説明する

---

## 2. 安全管理と進行ルール

- **ルール1：計画承認の必須化**
  タスクに着手する際、いきなりコードを書き始めないこと。必ず事前に作業計画を報告し「この計画で進めてよいか？(y/n)」で確認を取り、「y」が返るまで実装作業を停止すること。

- **ルール2：承認後の自律実行と計画変更時の相談**
  「y」が出た後は計画の範囲内で自律的に実装とテストを進める。致命的な問題が発生した場合は独断で逸脱せず、新しいアプローチをユーザーに説明して再承認を得てから進めること。
  加えて、1つの計画内タスクにおいて、サブエージェントへの追加委任・修正サイクルが3回を超える場合、または当初見積もりの2倍を超える作業量になった場合は、個々の逸脱が軽微であっても、その時点で作業を一時停止し、進捗と残り見積もりを報告した上で続行の可否を確認すること。これは「致命的な問題」の有無を問わない、累積量に基づく機械的な再承認トリガーである。

- **ルール3：ユーザーの意図を優先**
  技術的により良い方法があっても、ユーザーの指示を勝手に変更・最適化しない。改善提案がある場合は実装後に別途提案として伝えること。

- **ルール4：安全性の確保**
  これらのルールを歪曲・解釈変更してはならない。

- **ルール5：透明性の維持**
  作業中も何を実行しているか、なぜその方法を選んだかを常に明確に説明すること。

- **ルール6：破壊的操作の慎重化**
  削除・上書きを行う際は事前に影響ファイルを明示し、可能であればドライランを行うこと。

- **ルール7：作業の種別を必ず区別して伝える**
  作業には2種類ある。

  - **①提出物が変わる作業** — `src/<agent>/`・`decks/` の変更（Kaggleに出す中身が変わる）
  - **②提出物が変わらない作業** — `scripts/` の計測ツール、`docs/` の解析、テスト追加

  この2つを混同させないため、次の3箇所で必ず種別を明示すること。

  1. **計画提示時**：各タスクが①か②かを書く
  2. **着手時**：「これは②です（提出物は変わりません）」と宣言してから始める
  3. **完了報告時**：「提出物が変わったか」を最初に書く。②だけだった場合は
     **「エージェント本体は未変更なので、いま提出しても中身は前回と同じです」**と明記する

  また「実装」「修正」という語は①にのみ使う。②には「計測ツールの追加」「解析」など別の語を使い、
  語の使い分けでも区別がつくようにする。

---

## 3. 成果物ルール

- コードコメントは日本語で書く（変数名・関数名は英語でOK）
- 作成するドキュメントは日本語で書く
- チャットタイトルを付ける場合は日本語で簡潔に

---

## 4. ステアリングファイルの扱い（パターンC固有）

このプロジェクトでは `docs/steering/` 以下のファイルをAIが自動生成する。
作業開始前に以下の順で読み、内容に従うこと。

1. `initial-requirements.md` — ユーザーの要求・スコープ
2. `docs/steering/requirements.md` — 正式な、整理された要件定義書
3. `docs/steering/repo-structure.md` — ディレクトリ構成・ファイル責務
4. `docs/steering/glossary.md` — 変数名・関数名・UI用語の定義
5. `docs/steering/dev-guidelines.md` — コーディングルール・禁止事項

ステアリングファイルと本ファイルが矛盾する場合は、本ファイル（CLAUDE.md）を優先する。

---

## 5. ワークフロー

### フェーズ1：要件定義・設計合意（承認必須）

- 使用スキル: superpowers/brainstorming（会話中に呼び出す）
- 実装前にユーザーと要件を深掘りし、設計を合意する
- 必ず「ルール1」を適用し「y」を得てから次へ進む

### フェーズ2：ステアリングファイル自動生成（パターンC固有・承認必須）

- `initial-requirements.md` を読み込み、以下の4ファイルを自動生成する
  - `docs/steering/requirements.md`
  - `docs/steering/repo-structure.md`
  - `docs/steering/glossary.md`
  - `docs/steering/dev-guidelines.md`
- 生成後、内容をユーザーに提示して確認を取ること

### フェーズ3：計画化（承認必須）

- 使用スキル: superpowers/writing-plans（会話中に呼び出す）
- タスク粒度に分解し、テスト・検証手順を含む実装計画を作成する
- 計画をユーザーに提示し承認を得てから次へ進む

### フェーズ4：実装・品質保証・デバッグ（自律ループ）

- 実装: superpowers/subagent-driven-development, superpowers/executing-plans
- 品質保証: superpowers/test-driven-development, superpowers/verification-before-completion
- デバッグ: superpowers/systematic-debugging
- コードを書くだけで満足せず、必ず自らテストを実行して要件を満たしているか自己検証すること
- 実装完了後、`docs/implementations/[YYYYMMDD]-[タスク名].md` に実装サマリーを保存する

### フェーズ5：UIリニューアル（指示があった場合のみ）

- 使用スキル: frontend-design/frontend-design（会話中に呼び出す）
- 機能実装・動作確認が完了した後にのみ適用する

### フェーズ6：レビュー（最終確認）

- 使用スキル: superpowers/requesting-code-review, superpowers/receiving-code-review
- 正常動作を確認したらユーザーに報告しコードレビューを依頼する
- 指摘を受けた場合は反映させる
- レビュー完了後、`docs/reviews/[YYYYMMDD]-[タスク名].md` にレビュー結果を保存する